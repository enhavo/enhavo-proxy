parameters:
    locale: 'en'

imports:
    - { resource: services/command.yml }
    - { resource: services/general.yml }
    - { resource: services/form.yml }
    - { resource: services/manager.yml }

services:
    _defaults:
        autowire: true
        autoconfigure: true

